class Fibo{
    int fib(int n){
        if (n==0 || n==1) then
            return 1;
        fi
        return fib(n-1) + fib(n-2);
    }
    int main () {
    for(i=1 ; i<10 ; i++)
        print(fib(i));
        print("\n");
    rof
    return 0;
    }
}